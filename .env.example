# Required for tools/demo_web.py
# Generate local dummy values (never commit real secrets):
#
#   export ECHO_MARK_SECRET="$(python - <<'PY'
# import secrets
# print(secrets.token_hex(16))
# PY
# )"
#
#   export ECHO_MARK_PRIVATE_KEY="$(python - <<'PY'
# import secrets
# print(secrets.token_hex(32))
# PY
# )"
#
# Then copy values below (or source this file after replacing placeholders).
ECHO_MARK_SECRET=replace-with-generated-hex
ECHO_MARK_PRIVATE_KEY=replace-with-generated-hex
